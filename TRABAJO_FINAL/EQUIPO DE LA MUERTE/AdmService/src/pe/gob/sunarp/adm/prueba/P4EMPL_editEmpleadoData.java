package pe.gob.sunarp.adm.prueba;

import pe.gob.sunarp.adm.service.EMPLEADOService;

public class P4EMPL_editEmpleadoData {

    public static void main(String[] args) {

        // Datos
        String co_empl = "000007";
        String pat = "COTI";
        String mat = "COTI";
        String nom = "COTI";

        // Proceso
        /*EMPLEADOService service = new EMPLEADOService();
        service.editEmpleadoData(co_empl,pat, mat ,nom);

        // Reporte
        // Reporte
        if (service.getCode() == 1) {
            System.out.println(service.getMensaje());
        } else {
            System.err.println(service.getMensaje());
        }
         */
    }
}
